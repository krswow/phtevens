import { test, expect } from '@playwright/test';

// NFR-01 — Usability

test.skip('NFR-01-TC-01: Application is usable on mobile (375px width)', async ({ page }) => {
  // Not yet implemented: full UI required first
});

test.skip('NFR-01-TC-02: No content cut off when resized to 375px', async ({ page }) => {
  // Not yet implemented: full UI required first
});
