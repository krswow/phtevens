import { test, expect } from '@playwright/test';

// NFR-03 — Data Integrity

test.skip('NFR-03-TC-01: Member can edit their own coupon after submission', async ({ page }) => {
  // Not yet implemented: FR-08 (login) and FR-01 (coupon submission) required first
});

test.skip('NFR-03-TC-02: Member cannot set result of their own coupon', async ({ page }) => {
  // Not yet implemented: FR-08 (login) required first
});
