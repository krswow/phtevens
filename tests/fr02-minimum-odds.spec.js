import { test, expect } from '@playwright/test';

// FR-02 — Minimum Odds

test.skip('FR-02-TC-01: Coupon with total odds 3.0 is accepted', async ({ page }) => {
  // Not yet implemented: FR-08 (login) and FR-01 (coupon submission) required first
});

test.skip('FR-02-TC-02: Coupon with total odds 1.56 is rejected', async ({ page }) => {
  // Not yet implemented: FR-08 (login) and FR-01 (coupon submission) required first
});
